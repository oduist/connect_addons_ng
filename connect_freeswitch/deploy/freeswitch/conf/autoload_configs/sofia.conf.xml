<configuration name="sofia.conf" description="sofia Endpoint">
  <global_settings>
    <param name="log-level" value="$${sofia_loglevel}"/>
    <param name="debug-presence" value="0"/>
  </global_settings>
  <!-- Profiles served dynamically from Odoo via xml_curl configuration binding -->
  <profiles/>
</configuration>
