<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_freeswitch_endpoint_tree" model="ir.ui.view">
        <field name="name">connect.freeswitch.endpoint.tree</field>
        <field name="model">connect.freeswitch.endpoint</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <field name="connect_user_id"/>
                <field name="exten" invisible="connect_user_id"/>
                <field name="auth_user"/>
                <field name="originate_ring" invisible="not connect_user_id"/>
                <field name="auto_answer_header" optional="hide" invisible="not connect_user_id"/>
            </list>
        </field>
    </record>

    <record id="view_freeswitch_endpoint_form" model="ir.ui.view">
        <field name="name">connect.freeswitch.endpoint.form</field>
        <field name="model">connect.freeswitch.endpoint</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button name="create_extension" type="object" string="Extension"
                            class="btn btn-primary" invisible="connect_user_id"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <label for="name"/>
                        <h1>
                            <field name="name" placeholder="Endpoint Name"/>
                        </h1>
                        <label for="connect_user_id"/>
                        <h2>
                            <field name="connect_user_id" placeholder="Connect User"/>
                        </h2>
                    </div>
                    <group invisible="connect_user_id">
                        <field name="exten" readonly="1"/>
                    </group>
                    <group string="Authentication">
                        <field name="auth_user"/>
                        <label for="auth_password"/>
                        <div class="d-flex align-items-center">
                            <field name="auth_password" widget="endpoint_password" nolabel="1"/>
                            <button name="action_regenerate_auth_password" type="object"
                                    string="Regenerate" class="btn-link ms-2"
                                    icon="fa-refresh" invisible="not id"/>
                        </div>
                    </group>
                    <group string="Click-to-Call" invisible="not connect_user_id">
                        <field name="originate_ring" widget="boolean_toggle"/>
                        <field name="auto_answer_header" invisible="not originate_ring"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_freeswitch_endpoint_search" model="ir.ui.view">
        <field name="name">connect.freeswitch.endpoint.search</field>
        <field name="model">connect.freeswitch.endpoint</field>
        <field name="arch" type="xml">
            <search>
                <field name="name"/>
                <field name="connect_user_id"/>
                <separator/>
                <filter string="Standalone" name="standalone" domain="[('connect_user_id', '=', False)]"/>
                <filter string="Originate Ring" name="originate_ring" domain="[('originate_ring', '=', True)]"/>
                <filter string="Archived" name="archived" domain="[('active', '=', False)]"/>
            </search>
        </field>
    </record>

    <record id="action_freeswitch_endpoint" model="ir.actions.act_window">
        <field name="name">Endpoints</field>
        <field name="res_model">connect.freeswitch.endpoint</field>
        <field name="view_mode">list,form</field>
    </record>

    <menuitem id="menu_freeswitch_endpoints"
              name="Endpoints"
              parent="menu_freeswitch_root"
              action="action_freeswitch_endpoint"
              sequence="40"/>
</odoo>
