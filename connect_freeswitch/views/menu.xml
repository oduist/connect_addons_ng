<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- FreeSWITCH submenu of the Connect app. All provider submenus share
         the same sequence so they appear after Calls in installation order
         (equal sequence falls back to id order); the core Configuration menu
         (seq 100) always stays last. -->
    <menuitem id="menu_freeswitch_root"
              name="FreeSWITCH"
              parent="connect.menu_connect_root"
              sequence="50"/>

    <menuitem id="menu_freeswitch_config"
              name="Configuration"
              parent="menu_freeswitch_root"
              groups="connect.group_admin"
              sequence="100"/>
</odoo>
