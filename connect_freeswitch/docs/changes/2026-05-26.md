### Changed

- The firewall's health endpoint became a real readiness check instead of
  always answering OK.

### Fixed

- The freshest non-expired certificate is picked from Traefik's store, so a
  renewal no longer needs a manual restart.

- IVR DTMF menus, and leftover state after a queue was removed.
