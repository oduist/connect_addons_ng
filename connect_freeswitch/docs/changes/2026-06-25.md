### Fixed

- A system-wide default number is used as the outbound caller ID when nothing
  more specific applies.

- Server Status distinguishes its failure modes instead of reporting one
  generic error.
