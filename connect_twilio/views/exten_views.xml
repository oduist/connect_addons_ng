<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_twilio_exten_tree" model="ir.ui.view">
        <field name="name">connect.twilio.exten.tree</field>
        <field name="model">connect.twilio.exten</field>
        <field name="arch" type="xml">
            <list>
                <field name="number"/>
                <field name="dst"/>
                <field name="model_friendly" string="Type"/>
            </list>
        </field>
    </record>

    <record id="view_twilio_exten_form" model="ir.ui.view">
        <field name="name">connect.twilio.exten.form</field>
        <field name="model">connect.twilio.exten</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <group>
                            <field name="number"/>
                            <field name="dst"/>
                            <field name="model_friendly" string="Type" readonly="1"/>
                        </group>
                    </group>
                    <group string="Dialplan Preview" invisible="not dialplan">
                        <field name="dialplan" nolabel="1" colspan="2" readonly="1"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="action_twilio_exten" model="ir.actions.act_window">
        <field name="name">Extensions</field>
        <field name="res_model">connect.twilio.exten</field>
        <field name="view_mode">list,form</field>
    </record>

    <menuitem id="menu_twilio_extens"
              name="Extensions"
              parent="menu_connect_twilio"
              action="action_twilio_exten"
              sequence="20"/>
</odoo>
