<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="agent_tool_action" model="ir.actions.act_window">
        <field name="name">Tools</field>
        <field name="res_model">connect.elevenlabs_agent_tool</field>
        <field name="view_mode">list,form</field>
    </record>

    <menuitem id="connect.elevenlabs_agent_tool_menu"
              sequence="1300"
              parent="elevenlabs_menu"
              name="Tools"
              action="connect_elevenlabs.agent_tool_action"/>

    <record id="connect_elevenlabs_agent_tool_list" model="ir.ui.view">
        <field name="name">connect_elevenlabs_agent_tool_list</field>
        <field name="model">connect.elevenlabs_agent_tool</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <field name="tool_type"/>
                <field name="synced"/>
            </list>
        </field>
    </record>

    <record id="connect_elevenlabs_agent_tool_form" model="ir.ui.view">
        <field name="name">connect_elevenlabs_agent_tool_form</field>
        <field name="model">connect.elevenlabs_agent_tool</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <group>
                        <field name="name" readonly="tool_type == 'system'"/>
                        <field name="description"/>
                        <field name="tool_type" readonly="tool_type == 'system'"/>
                        <field name="tool_id" readonly="1" invisible="tool_type == 'system'" groups="base.group_no_one"/>
                        <field name="synced" readonly="1" invisible="tool_type == 'system'"/>
                        <field name="param_type" string="Parameters Type"
                               invisible="tool_type != 'webhook'"/>
                        </group>
                        <group>
                            <field name="path"
                                   invisible="tool_type != 'webhook'"/>
                            <field name="url"
                                   invisible="tool_type != 'webhook'"/>
                            <field name="method"
                                   invisible="tool_type != 'webhook'"/>
                            <field name="client_expects_response"
                                   invisible="tool_type != 'client'"/>
                            <field name="response_timeout_secs"
                                   invisible="tool_type == 'system'"/>
                            <field name="disable_interruptions"
                                   invisible="tool_type != 'system'"/>
                            <field name="voicemail_message"
                                   invisible="name != 'voicemail_detection'"/>
                            <field name="use_out_of_band_dtmf"
                                   invisible="name != 'play_keypad_touch_tone'"/>
                        </group>
                    </group>
                    <group string="Parameters" invisible="tool_type == 'system'">
                        <field name="body_params_description"
                               invisible="param_type != 'body'"
                               string="Body Parameters Description"/>
                        <field name="params" nolabel="1"
                               invisible="tool_type == 'system'"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

</odoo>
