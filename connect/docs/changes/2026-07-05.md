### Security

- Endpoints that a telephony server calls back into Odoo are authenticated,
  and the webhook grant was removed from the Twilio auth token, so one
  provider's credential no longer carries another's privileges.
