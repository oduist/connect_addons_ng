<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_connect_exten_tree" model="ir.ui.view">
        <field name="name">connect.exten.tree</field>
        <field name="model">connect.exten</field>
        <field name="arch" type="xml">
            <list>
                <field name="number"/>
                <field name="dst"/>
                <field name="model_friendly" string="Type"/>
            </list>
        </field>
    </record>

    <record id="view_connect_exten_form" model="ir.ui.view">
        <field name="name">connect.exten.form</field>
        <field name="model">connect.exten</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <group>
                            <field name="number"/>
                            <field name="dst"/>
                            <field name="model_friendly" string="Type" readonly="1"/>
                        </group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="action_connect_exten" model="ir.actions.act_window">
        <field name="name">Extensions</field>
        <field name="res_model">connect.exten</field>
        <field name="view_mode">list,form</field>
    </record>

    <menuitem id="menu_connect_extens"
              name="Extensions"
              parent="menu_connect_pbx"
              action="action_connect_exten"
              sequence="40"/>
</odoo>
