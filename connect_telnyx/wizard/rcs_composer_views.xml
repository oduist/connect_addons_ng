<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_telnyx_rcs_composer_form" model="ir.ui.view">
        <field name="name">connect.telnyx.rcs_composer.form</field>
        <field name="model">connect.telnyx.rcs_composer</field>
        <field name="arch" type="xml">
            <form string="Send RCS Message">
                <group>
                    <field name="rcs_agent_id"/>
                    <field name="phone" widget="phone"/>
                    <field name="body"/>
                    <field name="sms_fallback"/>
                    <field name="sms_fallback_from"
                   attrs="{'invisible': [('sms_fallback', '=', False)], 'required': [('sms_fallback', '!=', False)]}"/>
                    <field name="res_model" invisible="1"/>
                    <field name="res_id" invisible="1"/>
                </group>
                <footer>
                    <button name="action_send_rcs" string="Send" type="object" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_telnyx_rcs_composer" model="ir.actions.act_window">
        <field name="name">Send RCS Message</field>
        <field name="res_model">connect.telnyx.rcs_composer</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>
</odoo>
