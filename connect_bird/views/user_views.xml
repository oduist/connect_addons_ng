<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Inherit core user form to add Bird-specific fields -->
    <record id="view_connect_user_form_bird" model="ir.ui.view">
        <field name="name">connect.user.form.bird</field>
        <field name="model">connect.user</field>
        <field name="inherit_id" ref="connect.view_connect_user_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='active']" position="after">
                <field name="bird_phone_number"/>
                <field name="bird_voice_number"/>
                <field name="bird_message_number"/>
            </xpath>
        </field>
    </record>

</odoo>
